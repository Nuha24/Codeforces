#include<bits/stdc++.h>
#define ll long long
#define fastread()   (ios_base:: sync_with_stdio(false),cin.tie(NULL));
using namespace std;
int main()
{
    fastread();
    int m,n,a,b;
    cin>>m>>n>>a>>b;
    if(m>n)
        cout<<"First"<<endl;
    else
        cout<<"Second"<<endl;
}
